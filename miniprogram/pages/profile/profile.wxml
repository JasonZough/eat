<view class='page'>
    <image class='avator' src='{{person.avatarUrl}}' />
    <view class='info'>
        <view class='item'>{{person.name}}</view>
        <view class='item'>{{person.email}}</view>
    </view>
</view>